<a href="#main-content" class="skip-link">Aller au contenu principal</a>

<style>
  .skip-link {
    position: absolute;
    top: -100%;
    left: 16px;
    background: var(--ink);
    color: var(--paper);
    padding: 8px 16px;
    font-family: var(--font-mono);
    font-size: 13px;
    z-index: 100;
    transition: top 0.1s;
  }
  .skip-link:focus {
    top: 8px;
  }
</style>